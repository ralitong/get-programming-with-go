package main

func main() {
	type celsius float64
	type fahrenheit float64

	// var c celsius = 20
	// var f fahrenheit = 20

	// if c == f { // this is not possible since types can't be mixed even though they have
	//             // the same parent type float64

	// }
	// c += f
}